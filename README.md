# SoberSpace Site

Static landing page for the SoberSpace app screenshots.

## Start Locally

```bash
npm start
```

Then open:

```text
http://127.0.0.1:5173
```

If you do not want to use npm, run the same server command directly:

```bash
python3 -m http.server 5173 --bind 127.0.0.1
```
